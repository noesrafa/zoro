<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  launchd agent for zoro on macOS (the systemd equivalent of deploy/zoro.service).
  install.sh substitutes __ENGINE_DIR__ / __HOME__ and installs this to
  ~/Library/LaunchAgents/com.zoro.agent.plist. Runs as your user (LaunchAgent, not
  Daemon) so it can read ~/.claude credentials. The binary loads ./.env itself, so
  no EnvironmentFile is needed (launchd has no such directive).
-->
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.zoro.agent</string>

  <key>ProgramArguments</key>
  <array>
    <string>__ENGINE_DIR__/bin/zoro</string>
  </array>

  <key>WorkingDirectory</key>
  <string>__ENGINE_DIR__</string>

  <key>EnvironmentVariables</key>
  <dict>
    <key>PATH</key>
    <string>__HOME__/.local/bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
  </dict>

  <key>RunAtLoad</key>
  <true/>
  <key>KeepAlive</key>
  <true/>

  <key>ProcessType</key>
  <string>Background</string>

  <key>StandardOutPath</key>
  <string>__ENGINE_DIR__/state/zoro.out.log</string>
  <key>StandardErrorPath</key>
  <string>__ENGINE_DIR__/state/zoro.err.log</string>
</dict>
</plist>
